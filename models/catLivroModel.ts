export class catLivroModel {
    descricao: string;
    cat_id: string; // Código que será atrelado a categoria cadastrada
    dias_max: number; // Máximo de dias que a categoria pode ser alocada
    taxa_diaria: number; // Taxa diária por atraso
}